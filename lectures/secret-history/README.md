# Lectures — Secret History

Series partition under [`lectures/`](../README.md) for **`secret-history`** lecture chapters (`sh-*`).

Canonical Secret History packets live here under `lectures/secret-history/` (`sh-*`). Legacy `book/` paths are retained only as compatibility tombstones.

Catalog: [`docs/predictive-history-index.md`](../../docs/predictive-history-index.md).
