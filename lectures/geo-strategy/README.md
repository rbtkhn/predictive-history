# Lectures — Geo-Strategy

Series partition under [`lectures/`](../README.md) for **`geo-strategy`** lecture chapters (`geo-*`).

Canonical Geo-Strategy packets live here under `lectures/geo-strategy/` (`geo-*`). Legacy `book/` paths are retained only as compatibility tombstones.

Catalog: [`docs/predictive-history-index.md`](../../docs/predictive-history-index.md).
